const os = require("os");
console.log(os.userInfo());

console.log("os.totalmem()", os.totalmem());

console.log("os.freemem()", os.freemem());
console.log("os.freemem()", os.hostname());
console.log("os.freemem()", os.platform());

console.log("os.cpus()", os.cpus());
